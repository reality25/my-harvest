We already have a functioning Supabase backend, Edge Functions (ai-gateway, ai-diagnosis), authentication, weather service, news service, and AI assistant logic. Do NOT modify or break any backend logic, API keys, Supabase schema, or Edge Functions. Your task is to audit, fix, and redesign the frontend UI/UX so that all existing features are fully accessible, intuitive, and functional across mobile, tablet, and desktop.

The core problem is not missing backend capability but missing, hidden, or non-functional UI elements. You must create and properly connect all required buttons, navigation flows, and screens so users can actually use the system end-to-end. This includes clearly visible and working entry points for AI chat assistant, image diagnosis (camera/upload), farm management (records, activities, planner), weather, and agricultural news. AI, farm management, and other key features must have dedicated quick access actions from the Home screen and other logical entry points so users do not have to search for them.

Preserve the existing bottom navigation structure (Home, Market, My Farm, Community, Profile), but significantly enhance each section. The Home screen must act as a smart dashboard showing personalized weather, alerts, agricultural news, and clearly visible quick action buttons (Chat with AI, Diagnose via Photo, View Farm, Planner, Alerts). My Farm must include complete farm management capabilities such as viewing and editing farm records, managing farm entities (crops/livestock), tracking activities, and a fully functional farm planner with scheduling and reminders informed by weather and AI insights. Community and Market must remain intact but all interactions (posts, likes, comments, listings, navigation) must be fully functional and correctly routed.

Implement a clear and accessible AI Assistant interface where users can chat and also perform image-based diagnosis. Include a prominent “Take Photo / Upload Image” feature connected to the existing image analysis pipeline. Results must be displayed clearly with actionable insights and optionally linked to farm records.

Add a working global search feature that allows users to search across farm records, community posts, marketplace listings, and relevant content. Ensure all filters, buttons, tabs, and interactions across the app function correctly and trigger real behavior. There must be no dead buttons, placeholders, or UI elements that do nothing.

Create a proper Settings page accessible from the Profile section. This must include a working dark mode toggle, notification preferences, and language switching that uses the language selected during onboarding. Dark mode must apply globally, persist user preference, and maintain readability and layout integrity. Settings should be easy to access but not intrusive to the main workflow.

Ensure the entire interface is responsive, mobile-first, and intuitive. Improve layout, spacing, and hierarchy so that critical features are easy to find without cluttering the UI. Prioritize usability for real farmers who need fast access to weather, alerts, and farm actions.

All features must be connected to real data sources. Weather must reflect user location. News must come from verified APIs. AI must function through the backend Edge Functions. Farm management must read and write to Supabase. Do not introduce any hardcoded or mock data. If any service fails, handle it gracefully with proper loading and error states instead of silent failures.

Finally, perform a full integration audit to ensure that every feature (AI chat, image diagnosis, weather, news, farm management, planner, community, marketplace, profile, settings, search, alerts) is accessible, functional, and properly integrated. The goal is a complete, production-ready application where all systems work together seamlessly and nothing critical is missing or broken.