We already have a functioning Supabase backend, Edge Functions (ai-gateway, ai-diagnosis), authentication, weather service, news service, and AI assistant logic. Do NOT modify or break any backend logic, API keys, Supabase schema, or Edge Functions. Your task is to audit and rebuild the frontend UI/UX so that all existing features are fully accessible, intuitive, and functional for real users, especially farmers on mobile devices.

The current issue is not missing backend capability but missing or non-functional UI elements. You must create and connect all necessary buttons, navigation flows, and screens so users can actually use the system end-to-end. This includes clearly visible and working entry points for AI chat assistant, image diagnosis (camera/upload), farm management (records, activities, planner), weather, and agricultural news. Every button must either navigate to a proper page or trigger a working feature. No dead buttons, no placeholders, no disconnected components.

Preserve the existing bottom navigation structure (Home, Market, My Farm, Community, Profile), but enhance each section so it becomes fully functional and feature-complete. The Home screen should act as a smart dashboard showing personalized weather, key alerts, latest agricultural news, and quick actions (chat with AI, scan crop, view farm tasks). My Farm must include full farm management capabilities such as viewing farm records, adding/editing entities (crops/livestock), tracking activities, and a farm planner with scheduling and reminders based on weather and AI suggestions. Community and Market should remain intact but ensure all interactions (posts, likes, comments, listings) work correctly and navigate properly.

Implement a clearly accessible AI Assistant interface where users can chat naturally and also trigger image-based diagnosis. Include a prominent “Take Photo / Upload Image” action that connects to the existing image analysis pipeline. Ensure results are displayed clearly with actionable insights and tied back to farm records where relevant.

Add a functional search system that allows users to search across key areas such as farm records, community posts, marketplace listings, and possibly AI queries. Ensure filters work properly wherever they exist (e.g., filtering marketplace items, community content, farm activities). All interactive elements like buttons, filters, tabs, and toggles must be wired correctly and tested so they perform real actions.

Create a proper Settings screen accessible from the Profile section. This must include options such as editing profile/onboarding data, managing location preferences, toggling notifications, and enabling a dark mode. Dark mode must be implemented globally, persist user preference, and not break UI readability or layout. It should be easily accessible but not intrusive.

Ensure the entire UI is responsive and mobile-first, but also works cleanly on tablets and desktop screens. Navigation must feel natural with consistent spacing, readable typography, and clear hierarchy. Avoid clutter but do not hide critical features. Prioritize usability for real-world farmers who need quick access to weather, alerts, and farm actions.

All features must be connected to real data. Weather must reflect user location. News must be fetched from APIs. AI must respond via the backend. Farm management must read/write to Supabase. Do not introduce any hardcoded or mock data. If something fails, handle it gracefully with proper loading and error states instead of silent failures.

Finally, perform a full integration audit: confirm that every major feature (AI chat, image diagnosis, weather, news, farm management, planner, community, marketplace, profile, settings, search) is accessible from the UI, functions correctly, and interacts properly with the backend. The goal is a complete, production-ready interface where nothing important is hidden, broken, or missing.